[advisories]
ignore = [
    # paste: unmaintained, transitive dependency via tokenizers -> macro_rules_attribute.
    # No direct fix available until HuggingFace tokenizers drops the dependency.
    "RUSTSEC-2024-0388",
]
